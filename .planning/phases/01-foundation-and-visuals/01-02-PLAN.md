---
phase: 01-foundation-and-visuals
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/ASCIICake.tsx
  - src/components/FloatingHearts.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CAKE-01
  - CAKE-02
  - CAKE-03
  - BG-01
  - BG-02
  - BG-03
  - BG-04
  - BG-05
  - HEAD-01
  - HEAD-02
  - HEAD-03
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "ASCII cake is visible on screen with a monospace font and 3-frame candle flicker at ~4fps"
    - "28 hearts float upward from the bottom of the viewport in an infinite loop"
    - "Hearts use only blue #4a9eff and purple #9b59b6 — no pink or red"
    - "Hearts are non-interactive (pointer-events: none on container)"
    - "Hearts cycle through symbols: <3, ♥, ♡, ❤, ❥"
    - "Heading shows h1 '¡Feliz Cumpleaños!', badge 'Esclavo Rebelde', h2 'Fernando Banos' in that order"
    - "Card is centered with dark semi-transparent background and blur"
    - "Page works on 320px wide viewport without horizontal scroll"
  artifacts:
    - path: "src/components/ASCIICake.tsx"
      provides: "3-frame animated ASCII cake component with fps prop"
      exports: ["default ASCIICake"]
    - path: "src/components/FloatingHearts.tsx"
      provides: "28 animated floating hearts using CSS keyframes"
      exports: ["default FloatingHearts"]
    - path: "src/App.tsx"
      provides: "Root app with FloatingHearts + card (heading + ASCIICake)"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/FloatingHearts.tsx"
      via: "import + JSX render"
      pattern: "FloatingHearts"
    - from: "src/App.tsx"
      to: "src/components/ASCIICake.tsx"
      via: "import + JSX render inside .card"
      pattern: "ASCIICake"
    - from: "src/components/ASCIICake.tsx"
      to: "src/index.css .ascii class"
      via: "className='ascii'"
      pattern: "className.*ascii"
    - from: "src/components/FloatingHearts.tsx"
      to: "src/index.css .bg-hearts + .bg-heart + @keyframes floatHeart"
      via: "className='bg-hearts' + className='bg-heart'"
      pattern: "className.*bg-heart"
---

<objective>
Implement the two animated visual components — ASCIICake and FloatingHearts — and wire them into App.tsx to produce a fully animated, visually complete Phase 1 page.

Purpose: Deliver the entire visual foundation: animated cake, floating hearts background, heading with name/badge, all in blue/purple/black theme.
Output: ASCIICake.tsx, FloatingHearts.tsx, updated App.tsx. `npm run dev` shows the complete visual experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-visuals/01-CONTEXT.md
@.planning/phases/01-foundation-and-visuals/01-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01-01 that this plan builds on. -->

From src/index.css (already written in Wave 1):
  --color-blue: #4a9eff
  --color-purple: #9b59b6
  --color-white: #ffffff
  --color-card-bg: rgba(10, 10, 30, 0.85)
  .page — root layout container (main element)
  .card — centered card with blur and border
  .bg-hearts — fixed overlay, pointer-events none, z-index 0
  .bg-heart — absolute positioned heart span, bottom -80px, floatHeart animation
  .ascii — monospace pre, inline-block, clamp font-size
  .badge — pill label, purple border and text
  @keyframes floatHeart — translateY(-120vh) rotate(10deg), opacity fade 0→1→1→0

From src/App.tsx (Wave 1 shell):
  Renders: main.page > div.bg-hearts (empty) + section.card > h1 + span.badge + h2 + {cake placeholder comment}
  Imports: './index.css'

Source patterns (from CONTEXT.md canonical refs):
  ASCIICake source: https://raw.githubusercontent.com/ORYEPA/Feliz-Cum/main/src/components/ASCIICake.tsx
    - Props: { fps?: number } default 4
    - State: useState(0) for frame index
    - Effect: setInterval at 1000/fps ms, cleanup clearInterval
    - 3 frames in FRAMES array using String.raw, candle top row differs: "i i i" / "' * '" / "* ' *"
    - Returns: <pre className="ascii" role="img" aria-label="Pastel ASCII animado con velitas parpadeando">

  FloatingHearts source: https://raw.githubusercontent.com/ORYEPA/FB/main/src/App.jsx (bgHearts array section)
    - 28 items: useMemo(() => Array.from({ length: 28 }, (_, i) => ({...})), [])
    - Each item: id=i, symbol=heartSymbols[i % 5], left=(i*9)%100, delay=(i%10)*0.9, duration=10+(i%6)*2, size=20+(i%5)*8
    - heartSymbols = ['<3', '♥', '♡', '❤', '❥']
    - Rendered as: <span className="bg-heart" style={{ left, animationDelay, animationDuration, fontSize }}>

  Hearts CSS color (Wave 1 already defines .bg-heart — ONLY the color property is missing):
    Original FB: color rgba(255,215,232,0.35) — pink, DO NOT USE
    This project per D-09: blue hearts use rgba(74,158,255,0.35), purple hearts use rgba(155,89,182,0.35)
    Apply color per heart index: even index → blue, odd index → purple
    text-shadow per D-09: even → 0 0 12px rgba(74,158,255,0.4), odd → 0 0 12px rgba(155,89,182,0.4)
    Both colors stay well within blue/purple range — no pink or red (per D-12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ASCIICake component</name>
  <files>src/components/ASCIICake.tsx</files>

  <read_first>
    src/index.css — confirm .ascii class exists with font-family and className binding
    .planning/phases/01-foundation-and-visuals/01-CONTEXT.md — D-03 (cake below heading), CAKE-01..03
  </read_first>

  <action>
    Create directory src/components/ if it does not exist.

    Create src/components/ASCIICake.tsx as a direct TypeScript port of the source component from Feliz-Cum.

    The component must:
    - Export default function ASCIICake with props type { fps?: number } where fps defaults to 4
    - Declare a FRAMES constant (typed string[]) using String.raw for all three frames — do NOT escape backslashes manually
    - Frame 0 candle tops: "            i   i   i"
    - Frame 1 candle tops: "            '   *   '"
    - Frame 2 candle tops: "            *   '   *"
    - All three frames share identical cake body rows (candle stems | | | | | |, platform __|...|__, sides |...|, inner box with "Feliz" and "CUM" text)
    - Use useState(0) for frame index i
    - Use useEffect with setInterval at Math.max(1000 / fps, 60) ms, returns () => clearInterval(id) for cleanup
    - setInterval callback: setI(v => (v + 1) % FRAMES.length)
    - Return <pre className="ascii" role="img" aria-label="Pastel ASCII animado con velitas parpadeando">{FRAMES[i]}</pre>
    - Import only { useEffect, useState } from 'react'
    - Do NOT add any inline styles — the .ascii CSS class handles all styling

    The cake body text inside the box must read "Feliz" on one line and "CUM" on the next (matching source exactly). Do not change the ASCII art layout.
  </action>

  <verify>
    <automated>cd /home/peyro/Documents/Dev/fb-cum && npm run build 2>&1 | grep -E "error TS|Error" | grep -v "^#" || echo "BUILD_CLEAN"</automated>
  </verify>

  <done>src/components/ASCIICake.tsx exists and compiles without TypeScript errors. Component exports default ASCIICake. FRAMES has exactly 3 elements. useEffect returns a clearInterval cleanup. role="img" and aria-label are present.</done>
</task>

<task type="auto">
  <name>Task 2: FloatingHearts component and App wiring</name>
  <files>src/components/FloatingHearts.tsx, src/App.tsx</files>

  <read_first>
    src/index.css — confirm .bg-hearts, .bg-heart, @keyframes floatHeart exist
    src/App.tsx — current shell structure (Wave 1 output): main.page > div.bg-hearts + section.card
    .planning/phases/01-foundation-and-visuals/01-CONTEXT.md — D-09 (colors), D-12 (no pink/red), BG-01..05
  </read_first>

  <action>
    Create src/components/FloatingHearts.tsx:

    - Import { useMemo } from 'react'
    - Declare heartSymbols: string[] = ['<3', '♥', '♡', '❤', '❥']
    - Generate bgHearts with useMemo(() => Array.from({ length: 28 }, (_, idx) => ({
        id: idx,
        symbol: heartSymbols[idx % heartSymbols.length],
        left: (idx * 9) % 100,
        delay: (idx % 10) * 0.9,
        duration: 10 + (idx % 6) * 2,
        size: 20 + (idx % 5) * 8,
        isBlue: idx % 2 === 0
      })), [])
    - Return a div with className="bg-hearts" and aria-hidden="true"
    - Map bgHearts to span elements with:
        key={item.id}
        className="bg-heart"
        style={{
          left: item.left + '%',
          animationDelay: item.delay + 's',
          animationDuration: item.duration + 's',
          fontSize: item.size + 'px',
          color: item.isBlue ? 'rgba(74,158,255,0.35)' : 'rgba(155,89,182,0.35)',
          textShadow: item.isBlue ? '0 0 12px rgba(74,158,255,0.4)' : '0 0 12px rgba(155,89,182,0.4)'
        }}
      containing {item.symbol}
    - Export default FloatingHearts
    - Do NOT use any useState or useEffect — the animation is CSS-only
    - Do NOT include any pink/red color values (per D-12)

    Update src/App.tsx:

    - Add import FloatingHearts from './components/FloatingHearts'
    - Add import ASCIICake from './components/ASCIICake'
    - Replace the empty div.bg-hearts with <FloatingHearts /> (the component itself renders the div.bg-hearts)
    - Inside section.card, after the h2, render <ASCIICake fps={4} />
    - Remove the placeholder comment from Wave 1
    - Keep h1 "¡Feliz Cumpleaños!", span.badge "Esclavo Rebelde", h2 "Fernando Banos" exactly as in Wave 1 (per D-04, D-05, D-06, D-07)
    - Final card child order: h1 → span.badge → h2 → ASCIICake (per D-07)

    The App.tsx div.bg-hearts placeholder from Wave 1 must be removed — FloatingHearts renders its own div.bg-hearts internally, preventing duplicate containers.
  </action>

  <verify>
    <automated>cd /home/peyro/Documents/Dev/fb-cum && npm run build 2>&1 | grep -E "error TS|Error" | grep -v "^#" || echo "BUILD_CLEAN"</automated>
  </verify>

  <done>
    src/components/FloatingHearts.tsx exists, renders div.bg-hearts with 28 span.bg-heart children. Color values are blue rgba(74,158,255,0.35) and purple rgba(155,89,182,0.35) only — no pink or red hex values present. src/App.tsx imports and renders both FloatingHearts and ASCIICake. npm run build exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Animated ASCII cake (3-frame candle flicker at 4fps), 28 floating blue/purple hearts, heading with h1/badge/h2. All wired into App.tsx. npm run build passes.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open http://localhost:5173 in browser
    3. Confirm: page background is near-black (dark blue-black, #0a0a1a)
    4. Confirm: 28 hearts float upward from bottom continuously — they use symbols <3 ♥ ♡ ❤ ❥ — alternating blue and purple
    5. Confirm: no pink or red hearts visible anywhere
    6. Confirm: centered card with slight blur effect over the hearts
    7. Confirm: card shows "¡Feliz Cumpleaños!" (large h1), then "ESCLAVO REBELDE" pill badge in purple, then "Fernando Banos" subtitle, then ASCII cake
    8. Confirm: ASCII cake candles flicker — the top row changes between "i i i", "' * '", "* ' *" roughly every 250ms
    9. Resize browser to 320px width — verify no horizontal scroll bar appears
    10. Open browser console — confirm zero errors
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe which checks failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM | React renders into DOM; no user input accepted in Phase 1 |
| No external data | All content is hardcoded strings and CSS — no fetch calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Static SPA | accept | No secrets, no user data, no API keys; all content is birthday greeting text |
| T-02-02 | Denial of Service | setInterval in ASCIICake | mitigate | useEffect cleanup returns clearInterval — memory leak prevented on unmount |
| T-02-SC | Tampering | No new npm installs in this plan | accept | No new packages added; only component files created |
</threat_model>

<verification>
After all tasks complete (including human checkpoint):

1. npm run dev — page loads with all animations running, zero console errors
2. npm run build — exits 0, dist/ produced
3. grep -r "pink\|#ff\|rgba(255,.*,.*,\|red" src/ — returns no matches in component files (only blue/purple rgba values)
4. grep "FloatingHearts\|ASCIICake" src/App.tsx — both imports present
5. grep "clearInterval" src/components/ASCIICake.tsx — cleanup exists
6. grep "pointer-events" src/index.css — "pointer-events: none" present in .bg-hearts rule
</verification>

<success_criteria>
- ASCIICake renders 3-frame candle flicker, role="img" set, cleanup on unmount (CAKE-01, CAKE-02, CAKE-03)
- 28 hearts float upward infinitely in blue and purple only (BG-01, BG-02, BG-03, BG-04, BG-05)
- Heading shows "¡Feliz Cumpleaños!" h1, "Esclavo Rebelde" badge, "Fernando Banos" h2 (HEAD-01, HEAD-02, HEAD-03)
- Page works at 320px width without horizontal scroll (LAYOUT-03)
- ASCII cake visible without scrolling on a standard desktop viewport (LAYOUT-04)
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-visuals/01-02-SUMMARY.md` when done.
</output>
