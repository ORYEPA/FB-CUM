---
phase: 01-foundation-and-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - .gitignore
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - LAYOUT-01
  - LAYOUT-02

must_haves:
  truths:
    - "npm run dev starts a dev server at localhost:5173 without errors"
    - "npm run build produces a dist/ directory with zero TypeScript errors"
    - "App.tsx renders a single centered page (no routing)"
    - "Global CSS custom properties for blue/purple/black palette are defined"
  artifacts:
    - path: "src/App.tsx"
      provides: "Root application shell with centered card layout placeholder"
    - path: "src/index.css"
      provides: "Global styles: CSS variables, body background #0a0a1a, card base styles, reset"
    - path: "src/main.tsx"
      provides: "React 18 createRoot entry point"
    - path: "index.html"
      provides: "Vite HTML entry with lang=es and title"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "ReactDOM.createRoot render"
      pattern: "createRoot.*App"
    - from: "index.html"
      to: "src/main.tsx"
      via: "script type=module src=/src/main.tsx"
      pattern: "src/main.tsx"
---

<objective>
Scaffold the React + TypeScript + Vite project from scratch and establish the global CSS foundation — custom properties, body background, card shell — so Wave 2 can drop animated components directly into App.tsx without any wiring work.

Purpose: Prove the build toolchain works and establish the visual palette as CSS custom properties that all subsequent components consume.
Output: Working dev server, clean build, centered card shell, global CSS variables for blue/purple/black theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-visuals/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html, src/main.tsx, src/vite-env.d.ts, .gitignore</files>

  <read_first>
    .planning/phases/01-foundation-and-visuals/01-CONTEXT.md (stack decisions, D-08 through D-12)
    .planning/PROJECT.md (stack: React + TypeScript + Vite)
  </read_first>

  <action>
    Run: npm create vite@latest . -- --template react-ts

    Accept the overwrite prompt for the current directory. This produces the canonical Vite scaffold: package.json with react, react-dom, @types/react, @types/react-dom, typescript, vite dependencies; vite.config.ts with @vitejs/plugin-react; tsconfig.json and tsconfig.app.json; index.html entry; src/main.tsx with createRoot; src/vite-env.d.ts; .gitignore.

    After scaffolding, run: npm install

    Then update index.html:
    - Set lang="es" on the html element
    - Set title to "¡Feliz Cumpleaños, Fernando Banos!"
    - Remove any default Vite favicon link or replace href with "#" (no favicon asset needed yet)

    Clean up the scaffold defaults:
    - Delete src/App.css (styles go in src/index.css only)
    - Delete src/assets/react.svg
    - Delete public/vite.svg

    Do NOT touch src/App.tsx or src/index.css yet — those are handled in Task 2.
  </action>

  <verify>
    <automated>cd /home/peyro/Documents/Dev/fb-cum && npm run build 2>&1 | tail -5</automated>
  </verify>

  <done>npm run build exits 0 and produces dist/. index.html has lang="es" and title "¡Feliz Cumpleaños, Fernando Banos!". src/App.css and src/assets/react.svg do not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Global CSS and App shell</name>
  <files>src/index.css, src/App.tsx</files>

  <read_first>
    .planning/phases/01-foundation-and-visuals/01-CONTEXT.md (D-01 through D-12 — all locked color and layout decisions)
    The floatHeart keyframe source pattern from FB repo (recorded in CONTEXT.md canonical refs) — the @keyframes block will be added here so Wave 2 only needs to add the class usage
  </read_first>

  <action>
    Write src/index.css with:

    1. CSS custom properties on :root:
       --color-bg: #0a0a1a
       --color-blue: #4a9eff
       --color-purple: #9b59b6
       --color-white: #ffffff
       --color-card-bg: rgba(10, 10, 30, 0.85)
       --card-blur: blur(12px)

    2. Global reset and body:
       box-sizing: border-box reset on *, html body height 100%
       body: background-color var(--color-bg), color var(--color-white), font-family 'Segoe UI', Trebuchet MS, sans-serif, margin 0, min-height 100vh, display flex, align-items center, justify-content center

    3. .page class: position relative, min-height 100vh, width 100%, display flex, align-items center, justify-content center

    4. .card class (per D-01, D-02, D-10):
       background var(--color-card-bg), backdrop-filter var(--card-blur), -webkit-backdrop-filter var(--card-blur), border-radius 16px, padding 2.5rem 2rem, max-width 560px, width 90%, text-align center, position relative, z-index 1, border 1px solid rgba(74, 158, 255, 0.2), box-shadow 0 8px 32px rgba(0,0,0,0.6)

    5. .bg-hearts (per D-09, BG-01, BG-05):
       position fixed, inset 0, overflow hidden, z-index 0, pointer-events none

    6. .bg-heart (per D-09):
       position absolute, bottom -80px, margin 0, line-height 1, font-family 'Courier New' monospace, font-weight 700, animation-name floatHeart, animation-timing-function linear, animation-iteration-count infinite

    7. @keyframes floatHeart (ported exactly from FB source):
       0%: transform translateY(0) rotate(0deg), opacity 0
       10%: opacity 1 (times with value 0.35 applied via color, not here)
       90%: opacity 1
       100%: transform translateY(-120vh) rotate(10deg), opacity 0

    8. .ascii class (for cake):
       font-family 'Courier New', Courier, monospace, font-size clamp(0.55rem, 1.5vw, 0.85rem), line-height 1.2, color var(--color-white), text-align left, display inline-block, margin 0

    9. Heading styles:
       h1: font-size clamp(1.6rem, 4vw, 2.4rem), color var(--color-white), margin 0 0 0.5rem, font-weight 700
       h2: font-size clamp(1rem, 2.5vw, 1.4rem), color var(--color-blue), margin 0.25rem 0 1rem, font-weight 400

    10. .badge class (for "Esclavo Rebelde" pill, per D-05):
        display inline-block, font-size 0.72rem, font-weight 600, text-transform uppercase, letter-spacing 0.08em, color var(--color-purple), border 1px solid var(--color-purple), border-radius 999px, padding 0.2rem 0.75rem, margin 0.4rem 0

    Write src/App.tsx:

    Import React (no default needed with modern JSX transform, but keep explicit for clarity).
    Import './index.css'.

    Return JSX:
      main element with className="page"
        div with className="bg-hearts" aria-hidden="true" — empty for now, FloatingHearts component will be added in Wave 2
        section with className="card"
          h1 content "¡Feliz Cumpleaños!" (per D-04)
          span with className="badge" content "Esclavo Rebelde" (per D-05)
          h2 content "Fernando Banos" (per D-06)
          comment: ASCII cake goes here in Wave 2

    Do NOT import FloatingHearts or ASCIICake — those components do not exist yet.
    Do NOT add any pink or red color values anywhere (per D-12).
  </action>

  <verify>
    <automated>cd /home/peyro/Documents/Dev/fb-cum && npm run build 2>&1 | grep -E "error|Error|ERROR" | grep -v "^#" || echo "BUILD_CLEAN"</automated>
  </verify>

  <done>npm run build exits 0. src/index.css defines --color-bg, --color-blue, --color-purple, --color-card-bg, .card, .bg-hearts, .bg-heart, @keyframes floatHeart, .ascii, .badge. src/App.tsx renders the three heading elements (h1, .badge, h2) inside .card inside .page. No pink/red values exist in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Third-party packages fetched from registry — supply chain risk |
| Vite scaffold | npm create vite downloads scaffold code at run time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (react, vite, typescript) | accept | All packages are major, widely-used, non-niche; versions pinned by Vite scaffold template; no `[ASSUMED]` or `[SUS]` packages |
| T-01-SC | Tampering | npm/vite scaffold installs | accept | react, react-dom, vite, @vitejs/plugin-react, typescript are industry-standard packages with millions of weekly downloads; no legitimacy concerns |
</threat_model>

<verification>
After both tasks complete:

1. npm run dev — dev server starts, browser shows centered card with h1/badge/h2, near-black background
2. npm run build — exits 0, dist/ directory produced
3. No console errors in browser dev tools
4. Page background is #0a0a1a (visually very dark blue-black), not white or grey
5. No pink or red visible anywhere on the page
</verification>

<success_criteria>
- npm run dev and npm run build both exit 0
- Vite scaffold files exist: package.json, vite.config.ts, tsconfig.json, src/main.tsx
- src/index.css defines all 6 CSS custom properties (--color-bg, --color-blue, --color-purple, --color-white, --color-card-bg, --card-blur)
- src/App.tsx renders h1 "¡Feliz Cumpleaños!", span.badge "Esclavo Rebelde", h2 "Fernando Banos"
- index.html has lang="es" and correct title
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-visuals/01-01-SUMMARY.md` when done.
</output>
